mixin heroSection(content)
  section.hero
    .swiper.banner.hero-slider
      .swiper-wrapper
        each item, index in content.slides
          .swiper-slide.hero__wrapper(class= item.light ? 'hero__wrapper-light' : '')
            if (item.background)
              .hero__background
                .hero__background-overlay
                img(src=require(`../../assets/images/${item.background.src}`), alt= item.background.alt ? item.background.alt : '').hero__background-image

            .container.hero__container
              - const tag = (index === 0) ? 'h1' : 'h2'
              #{tag}.banner-title.hero__title(class= item.shortTitle ? 'hero__title--short' : '')= item.title

              if (item.text)
                .hero__text
                  p.banner-text !{item.text}

              if (item.nav)
                .hero__nav
                  each link in item.nav
                    a(href= `#${link.href}`).js-scroll-link.navlink.hero__nav-link= link.text 

              if (item.link)
                .hero__link
                  +link(item.link)

              if (item.socials)
                .hero__socials
                  +socials

      .swiper-pagination.hero-slider__pagination
